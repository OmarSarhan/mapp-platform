# Installed with --no-deps in the Dockerfile. Every module this component
# imports lives under authlib.oauth2.rfc6749/6750/7009/7636/7662/8414/9207,
# none of which import joserfc or cryptography, so the component stays free of
# native dependencies on musl. test_no_native_dependencies pins that.
Authlib==1.8.0
