# Validation log

This is a bounded acceptance log inspired by the requested autoresearch
workflow. `keep` means the change met its gate; `blocked` means the gate could
not complete because of an environmental or external dependency, not that it
passed.

The 2026-07-15 entries predate the platform/CLI directory split and the
`instance`/`var` state boundary. They are historical evidence only. Later rows
state explicitly which final split paths and runtime checks passed; no row
establishes unlisted production, backup, or restore acceptance.

| Date (UTC) | Change or baseline | Gate | Result | Evidence |
| --- | --- | --- | --- | --- |
| 2026-07-15 | Pin XYZ v4.23.4 | Tag resolves to expected full commit | keep | `a6f03c07dd7aaae2e9ab04087143ee0400e15cb9`; Node 22+ requirement and build command checked in upstream source/wiki |
| 2026-07-15 | Build pinned XYZ and load mounted workspace | Declared pnpm build succeeds; XYZ resolves all instance layers | keep | Node 22.23.1 + pnpm 10.10.0 build passed; local XYZ returned HTTP 200 and resolved OSM plus all three MVT layers with `DBS_MAPP` |
| 2026-07-15 | Select Leeds sample | Point, line, polygon plus varied field types; bounded layer set | keep | live metadata/count/GeoJSON checks: 4,233 bus stops, 2,484 paths, 275 recent planning records |
| 2026-07-15 | ETL transforms and consistency logic | Unit and SQL-composition suite | keep | 21 tests passed in a fresh environment; count drift, duplicate IDs, and concurrent runs skip reconciliation |
| 2026-07-15 | ETL concurrency | Overlapping same-layer runs cannot reconcile each other's rows | keep | two live PostgreSQL sessions confirmed the second lock is rejected until the first releases; the competing pipeline exits before any run record or data write |
| 2026-07-15 | Pin ETL runtime | Exact Python and database-driver releases | keep | ETL image uses `python:3.12.13-slim-bookworm`; `psycopg[binary]` is pinned to the tested 3.3.4 release |
| 2026-07-15 | Leeds source contract | Opt-in live suite and `--check-source` | keep | all three layers reported correct metadata, EPSG:27700, GeoJSON support, and expected geometry families |
| 2026-07-15 | ETL database integration | Two complete least-privilege PostGIS imports | keep | 6,992 rows per run, zero invalid geometries, correct 4326/3857 SRIDs, six GiST indexes, zero deletions or changed-hash timestamps on identical rerun (PostgreSQL 15/PostGIS 3.3 harness) |
| 2026-07-15 | XYZ/PostGIS integration | Load all layers with ETL role, query with read-only XYZ role | keep | local PostgreSQL/PostGIS harness loaded 4,233 + 2,484 + 275 rows; XYZ returned HTTP 200 and a 38,183-byte Bus Stops MVT |
| 2026-07-15 | Compose topology | Parse and normalize with Docker Compose v5.3.1 | keep | base file and optional DB-port override both passed `config --quiet` |
| 2026-07-15 | Rebuild/recreate behavior | Framework and ETL upgrades do not leave stale processes | superseded | XYZ now uses a generation-based child-process supervisor; Caddy remains stable during workspace reloads |
| 2026-07-15 | Acceptance timing | Fresh containers can reach healthy without a false-negative race | keep | verifier polls each required service for up to 60 seconds and still fails immediately on a terminal container state |
| 2026-07-15 | Database initialization/readiness | Least-privilege bootstrap and full initialization health gate | keep | init script created both roles and the ETL-owned schema in a fresh PostgreSQL harness; health now waits for PostGIS, roles, schema, and runtime marker |
| 2026-07-15 | Full container startup | Build images, load PostGIS, rerun ETL, query XYZ | superseded | that execution environment lacked Docker access; the successful 2026-07-16 split-stack run below supersedes this historical limitation |
| 2026-07-16 | Final split component suites | Platform, CLI, packaging, typing, frontend, dependency, syntax, and Compose checks | keep | platform: 61 configuration tests, 23 ETL tests with 1 opt-in live test skipped, 13 wrapper/database/production tests with 1 host-Node comparison skipped, 4 frontend tests/build, npm audit with 0 findings, and Compose validation; CLI: 62 tests, mypy clean, compilation, wheel/sdist build, `twine check`, and fresh wheel install |
| 2026-07-16 | Browser-runner capacity gate | Bound simultaneous Chromium work, reject excess work, and release the slot | keep | black-box test held the only configured slot, received HTTP 429 for a competing run, then confirmed a later run was accepted after slot release |
| 2026-07-16 | Live split stack | Rebuild, service health, database, guards, workspace identity, reload, and XYZ child topology | keep | `db`, `xyz`, `config-ui`, `browser-runner`, and `caddy` healthy; `./bin/mapp verify` passed; 4,233 bus stops, 2,484 paths, and 275 planning rows retained; live, seed, and backup workspace SHA-256 matched; reload generation completed healthy with exactly one XYZ application child |
| 2026-07-16 | Standalone CLI acceptance | Temporary token, remote inspection, dry run, XYZ status, and authenticated visual test | keep | CLI reported the expected instance/revision and Bus Stops layer, dry run explicitly returned `saved: false`, XYZ was healthy, Chromium visual test passed, and the temporary token was revoked |
| 2026-07-16 | Leeds live source recheck | Metadata, count, and GeoJSON query for every configured ETL layer | blocked | bus and path sources passed; `Planning/MapServer/1` returned ArcGIS error 400 `Failed to execute query`; ETL safety logic retained the existing 275-row snapshot and no alternative planning layer was substituted |
| 2026-07-16 | Bundled/external PostGIS routing | One shared runtime URI, isolated sample services, mode-aware lifecycle and verification | keep | minimal external Compose omitted both `db` and `etl`; bundled Compose retained their health dependencies; resolved `DBS_MAPP` was identical in XYZ and config UI; external commands rejected sample ETL/local DB operations and bundled/loopback database hosts; bundled live verification retained all 6,992 sample rows |
| 2026-07-16 | Live external PostGIS acceptance | Connect both application containers to a genuinely remote PostGIS service and run workspace-specific visual tests | blocked | no separate external database was available in this environment; an initial run against the existing bundled service was rejected as invalid evidence and prompted the explicit external-host guard |
| 2026-07-16 | HTTPS-first production topology | Persist production mode; require public Caddy bindings and HTTPS origins; retain system trust in both database clients | keep | production validation and Compose resolution passed with direct ports 80/443 and distinct HTTPS hosts; Caddyfile validation enabled automatic TLS and HTTP-to-HTTPS redirects; both routes served the one-year HSTS policy; built XYZ and config UI images contained the system CA bundle; rebuilt bundled stack passed live verification; public certificate issuance still requires the intended DNS host |
| 2026-07-17 | Standalone CLI professionalization | Full unit, contract, security, presentation, loopback-integration, typing, and packaging suite after checked-proposal handoff and fail-fast argument validation | keep | 101 tests passed with zero skips, including profile-scoped check caches, unsafe-state doctor preflight, setup compare-and-swap/rollback and confirmed-instance race guards, superseded-credential cleanup, server-owned effective-layer inspection, JSON-safe prompts, transport remediation, and exact nested shell completion paths; mypy reported no issues in 11 source files; generated Bash, Zsh, and Fish scripts passed their real shell parsers; wheel and sdist built in isolation; `twine check` passed; a fresh wheel install reported version 0.1.0 |
| 2026-07-17 | Previously Node-gated platform checks | Build the top-level and both repository devcontainers with exact Node 22.23.1 where required, then run the complete helper suite | keep | all three devcontainer images built; all 17 helper/database/production tests passed with zero skips, including the Python/Node saved-byte fingerprint comparison, strict database-host guard, strict HTTPS-origin validation, and non-root production UID/GID guard; 3 ETL database SQL tests also passed directly |
| 2026-07-17 | Opt-in live Leeds source suite | Remove the live-test skip and query metadata, count, and GeoJSON for every configured layer | blocked | the test ran; bus stops and definitive paths passed, but `Planning/MapServer/1/query` again returned ArcGIS error 400 `Failed to execute query`; the loader remains fail-closed and no replacement source was inferred |
| 2026-07-17 | Final split-repository regression | Re-run application, packaging, frontend, Compose, Caddy, and live bundled-stack gates after review fixes | keep | 63 configuration-service tests, 23 non-live ETL unit/SQL checks plus 1 deliberately opt-in live test, 17 zero-skip helper tests, 4 frontend tests/build, both npm production audits with 0 findings, Node syntax checks, bundled/external/production Compose variants, Caddy validation, and `./bin/mapp verify` passed; the opt-in live ETL check was separately executed and is recorded as blocked above; bundled verification retained 4,233 + 2,484 + 275 rows |
| 2026-07-17 | Managed reload feedback and remote alias | Confirm exact-fingerprint reload coordination, dashboard restart/readiness messages, and the standalone reload alias | keep | all 74 configuration-service tests and 10 frontend tests passed; the production dashboard build completed; all 108 standalone CLI tests passed with zero skips; mypy remained clean across 11 source files; the fingerprint-bound local reload command completed against the live stack; both repository diffs passed whitespace checks; the rebuilt bundled stack returned healthy and `./bin/mapp verify` passed with its retained 4,233 + 2,484 + 275 sample rows |
| 2026-07-17 | Isolated proposal preview and production-evidence harness | Exercise a real pending proposal through candidate plan/test endpoints without publishing it, and truthfully classify locally observable production controls | keep | a temporary-token run returned `source: candidate`, preserved proposal and candidate-hash bindings, passed the Bus Stops browser test, and left the live workspace revision unchanged before declining the proposal and revoking the token; the current development environment report recorded 1 pass, 3 failures, and 12 pending controls, while a synthetic production-static fixture recorded 4 passes, 0 failures, and 12 pending controls; neither report claims live infrastructure or rehearsal acceptance |
| 2026-07-17 | Agent-facing server interaction contract | Verify dynamic action discovery, scoped authorization, proposal preflight, structured visual diagnosis, durable operation lookup, request correlation, and denial of ungranted authority | keep | 110 standalone CLI tests and mypy passed; 74 configuration-service tests, complete platform/ETL/helper/frontend/build/audit/Compose gates, and live `./bin/mapp verify` passed; a temporary `inspect + propose + visual` token discovered schemas, completed a revision-bound preflight, produced a successful Bus Stops operation with structured diagnosis, and received exit 7 plus `auth.scope_required` when attempting reload; the token was revoked |
| 2026-07-17 | Final lifecycle, scoped-auth, and cross-repository contract hardening | Fail closed across concurrent workspace replacement, corrupt reload state, process interruption, device issuance, proposal approval, and visual-operation exceptions | keep | all 83 configuration-service tests, 10 frontend tests, and 110 standalone CLI tests passed with zero skips; the production dashboard build completed; mypy reported no issues in 11 CLI source files; approved device polling persisted only a hash and the legacy-raw migration test revoked staged orphan credentials; the CLI sent the server-advertised `approved: true` apply payload; the final rebuilt bundled stack passed Caddy-routed verification with 4,233 + 2,484 + 275 rows, and fingerprint-bound reload generation 9 completed for the exact live workspace |
| 2026-07-17 | Replace failed Leeds polygon sample and normalize expected source errors | Exact ArcGIS error regression, zero-skip live source suite, complete bundled import, seed catalog/render validation, and mutable-workspace verification | keep | the broken 275-row planning source was retired without dropping its snapshot; `Planning/MapServer/8` loaded 200 Smoke Control Orders under a distinct key/table after 11/12 were rejected at 65,642/363,050 rows; all 29 ETL tests passed with the live test enabled, all 83 configuration-service tests passed, the dashboard validator returned no seed errors, `./bin/mapp etl` loaded 4,233 + 2,484 + 200 rows, and `./bin/mapp verify` passed while selecting an MVT layer from the user-edited live workspace; expected ArcGIS service errors retain exit 1 but no longer print a traceback |
| 2026-07-17 | Proposal approval screenshot comparison | Render the retained original and candidate at one view, retain exact bounded dimensions, and require the expanded feature-information panel when relevant | keep | `./bin/mapp test` passed 91 configuration-service, 29 ETL (1 opt-in live skip), 23 helper, 7 frontend API, and 3 dashboard component tests plus the production build, audits, and Compose checks; after reducing the default, a live isolated Bus Stops comparison returned HTTP 200 with exact 1080×1080 original and candidate page images at 1× scale and visibly changed red letter markers into gold dots; a selected-feature run at the same settings confirmed the expanded panel and retained a 330×320 panel artifact; the temporary visual tokens were revoked |

The final local checks above do not replace production acceptance. Record
resolved image digests, production Postgres/PostGIS versions, ETL durations,
first/second-run counts, MVT smoke results, TLS behavior, and restore evidence
when the release is exercised on its intended infrastructure.
